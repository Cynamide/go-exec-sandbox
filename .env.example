# Docker Compose uses this to pull the local model on startup.
# The evaluator runtime model is configured in benchmark.yaml.
OLLAMA_MODEL=qwen3:4b
# Optional: Ollama host URL (default: http://localhost:11434)
OLLAMA_HOST=http://localhost:11434
